import pygame

class Sprite:
    def __init__(self, filename):
        self.size = spriteSize
        self.art = pygame.image.load(filename).convert()
        return
    def draw(x,y):
        return
